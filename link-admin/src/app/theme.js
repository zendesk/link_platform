import { DEFAULT_THEME } from '@zendeskgarden/react-theming'

const theme = {
  ...DEFAULT_THEME,
  sidebar: {
    width: {
      closed: '65px',
      open: '150px'
    }
  }
}

export default theme
